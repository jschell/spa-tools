This tool parses and displays SIGMA detection rules in a structured, readable format. Paste a SIGMA rule in YAML format and the viewer will validate its schema, highlight required and optional fields, and show which Microsoft Sentinel log tables are relevant based on the rule's logsource. It also generates a KQL query scaffold based on the rule's detection logic, giving you a starting point for translating the SIGMA rule into a Sentinel analytics rule.
